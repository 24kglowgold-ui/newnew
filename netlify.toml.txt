[build]

Specifies the directory where Netlify can find your serverless functions

functions = "netlify/functions"
publish = "." # The base directory for files to deploy (index.html is here)

Redirects section to route client API calls to the serverless function

[[redirects]]

The path the client-side JavaScript calls

from = "/api/gemini"

The actual path of the Netlify Function

to = "/.netlify/functions/gemini-proxy"

Ensures the full path and method are proxied

status = 200